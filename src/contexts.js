import { createContext } from 'react'

export const GlobalDependenciesContext = createContext(null)

export const LoginFormContext = createContext(null)
export const RegistrationFormContext = createContext(null)

export const GoodDialogContext = createContext(null)

export const CanbanContext = createContext(null)
export const FeedContext = createContext(null)
